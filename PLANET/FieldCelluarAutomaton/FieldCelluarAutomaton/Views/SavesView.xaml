<UserControl x:Class="FieldCelluarAutomaton.Views.SavesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FieldCelluarAutomaton.Views"
             xmlns:viewModels="clr-namespace:FieldCelluarAutomaton.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:SavesViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <ListBox x:Name="SavesList" ItemsSource="{Binding Saves}" Background="#131313" Foreground="White">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <UserControl MouseDoubleClick="Control_OnMouseDoubleClick" Tag="{Binding }">
                    <TextBlock Foreground="White" Text="{Binding }"/>
                </UserControl>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
